package com.benchreadiness.ops.feature;

/** Mirrors com.benchreadiness.auth.feature.FeatureKey in auth-service. Only COMPLIANCE is enforced here. */
public enum FeatureKey {
    INTERVIEWS, REVIEW, SCREENING, CANDIDATES, BULK_IMPORT, DEPLOYMENT_IMPORT,
    CLIENTS, RECRUITER_BOT, CALENDAR, COMPLIANCE, MASTER_DATA, QUESTION_BANK, ANALYTICS
}
