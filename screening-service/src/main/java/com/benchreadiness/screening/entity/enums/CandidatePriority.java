package com.benchreadiness.screening.entity.enums;

/** Recruiter-assigned priority after Round 1, carried through to the Round 2/3 queues. P0 = highest. */
public enum CandidatePriority {
    P0,
    P1,
    P2,
    P3
}
