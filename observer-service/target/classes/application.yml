server:
  port: 6007

spring:
  application:
    name: observer-service
  datasource:
    url: jdbc:postgresql://localhost:3308/bench_readiness
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: observer_svc
  flyway:
    schemas: observer_svc
    locations: classpath:db/migration
  mail:
    host: smtp.gmail.com
    port: 587
    username: ariprasath6767@gmail.com
    password: xkvt ujsd xvvp nszz
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6009/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30

app:
  jwt:
    secret: dev-jwt-secret-change-in-production-min-32-chars
  interview-base-url: http://localhost:6001/interview
  frontend-url: http://localhost:6001
  digest:
    cron: 0 0 19 * * *
